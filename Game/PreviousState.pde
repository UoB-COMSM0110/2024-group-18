class PreviousState {
  PVector location;
  PImage animation;
  
  /* Stores location and animation of an object for reversal. */
  PreviousState(PVector location, PImage animation) {
    this.location = location;
    this.animation = animation;
  }
  
}
